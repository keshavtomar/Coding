// bitwise operator is used in binary number
// & -->>bitwise and operator 
// | -->> bitwise or operator
// << it is left shift operator. for example 2<<1=4(it change first 2 in binary then shift their respective position by 1)
// in general a<<b=a*pow(2,b)
// >> it is right shift operator. for example 32>>1=16
// in geeneral a>>b=a/pow(2,b)
//  ^ it is xor operator:
// XOR of two same number is always 0
// when a number is XOR with 0 it gives the number itself.
// ~ it is used to flip the bits